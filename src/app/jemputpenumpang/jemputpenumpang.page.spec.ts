import { ComponentFixture, TestBed } from '@angular/core/testing';
import { JemputpenumpangPage } from './jemputpenumpang.page';

describe('JemputpenumpangPage', () => {
  let component: JemputpenumpangPage;
  let fixture: ComponentFixture<JemputpenumpangPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(JemputpenumpangPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
