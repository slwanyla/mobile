import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MenunggudriverPage } from './menunggudriver.page';

describe('MenunggudriverPage', () => {
  let component: MenunggudriverPage;
  let fixture: ComponentFixture<MenunggudriverPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(MenunggudriverPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
