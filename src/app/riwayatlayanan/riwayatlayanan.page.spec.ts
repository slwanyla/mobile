import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RiwayatlayananPage } from './riwayatlayanan.page';

describe('RiwayatlayananPage', () => {
  let component: RiwayatlayananPage;
  let fixture: ComponentFixture<RiwayatlayananPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(RiwayatlayananPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
