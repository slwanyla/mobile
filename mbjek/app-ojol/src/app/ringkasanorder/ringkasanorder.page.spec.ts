import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RingkasanorderPage } from './ringkasanorder.page';

describe('RingkasanorderPage', () => {
  let component: RingkasanorderPage;
  let fixture: ComponentFixture<RingkasanorderPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(RingkasanorderPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
