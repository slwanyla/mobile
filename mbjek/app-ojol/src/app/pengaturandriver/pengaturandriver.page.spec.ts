import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PengaturandriverPage } from './pengaturandriver.page';

describe('PengaturandriverPage', () => {
  let component: PengaturandriverPage;
  let fixture: ComponentFixture<PengaturandriverPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(PengaturandriverPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
