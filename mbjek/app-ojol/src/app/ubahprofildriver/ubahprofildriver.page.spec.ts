import { ComponentFixture, TestBed } from '@angular/core/testing';
import { UbahprofildriverPage } from './ubahprofildriver.page';

describe('UbahprofildriverPage', () => {
  let component: UbahprofildriverPage;
  let fixture: ComponentFixture<UbahprofildriverPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(UbahprofildriverPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
